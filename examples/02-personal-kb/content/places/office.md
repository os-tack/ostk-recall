# The Office

A small room with too many whiteboards and one reliable coffee machine. Where the standups
happen.

A `place` node. It has no outgoing edge vocabulary of its own (no `edges` on the places
source), but meetings point *at* it — so it accrues incoming edges and becomes findable by
traversal from any meeting held here.
