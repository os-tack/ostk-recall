# Decision: derive conductance

The agent's durable record of a decision it participated in: edge conductance is **derived**
(`confidence × recency`), never stored. Authoring sets the topology; use sets the conductance.

This is a leaf note — nothing points out of it via `relates_to`, but `project-ostk` points in.
A decision the agent can recall, with provenance, in any future session.
